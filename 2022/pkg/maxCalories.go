package maxCalories

func FindMaxCalories(list string) int {
	// Find the number of calories carried by the elf carrying the most calories
	// elfCalories := make([]int, 6)

	for key, value := range list {
		println(key, value)
	}
	return 0
}
